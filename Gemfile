source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-include-cache"
end

# Required for `bundle exec jekyll serve` on Ruby 3.x
gem "webrick", "~> 1.7"
